services:
  lungsight:
    build: .
    image: ${DOCKERHUB_USERNAME:-jeevanms003}/lungsight:latest
    ports:
      - "7860:7860"
    environment:
      - DATA_DIR=/app/data
      - GRADIO_SERVER_NAME=0.0.0.0
      - GRADIO_SERVER_PORT=7860
    volumes:
      - ./data:/app/data
